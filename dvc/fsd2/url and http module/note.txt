http module : 

- server listens the request from our code and it read the code and then fetch the required files and then it renderes those file in the browers and this is how website work and which is why url bridges the request and responses or server to browser...

res.end() = it can also send the data as well as terminate browser, once closed it cannot accepts further data and hence we will get the error...

- node js http module does understand string in res.write() but we have to specify what kind of file we want to render on the browser sp we have to write or we have to teach node js to consider the whole file of say for eg : html = 

res.writeHead(status code = 200 (*means successful code), json obj (*{"Content-type" : "text/htm;"}))

// how to read request objevt and its key ? 
- res.end(req.url) 